# 1. 将传感器工作队列堆栈翻倍，防止高速中断爆栈丢包
CONFIG_ZMK_SENSOR_WORKQUEUE_STACK_SIZE=4096

# 2. 提高传感器中断处理优先级，确保脉冲被及时消费
CONFIG_ZMK_SENSOR_INIT_PRIORITY=80

# 3. 明确指定编码器驱动队列深度（若驱动支持）
CONFIG_ZMK_SENSOR_ENCODER_BUFFER_SIZE=64
